# PartPatch

PartPatch is a local-first service for custom 3D-printed replacement parts: the small components that are difficult to source and expensive to replace as part of a larger assembly.

## Development

```powershell
npm install
npm run dev
```

Run `npm run build` before deploying. The current quote form is an interactive prototype and does not send or retain customer details.
